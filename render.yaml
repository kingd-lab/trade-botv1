services:
  - type: web
    name: paper-bot-v3
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    disk:
      name: paper-bot-data
      mountPath: /data
      sizeGB: 1
